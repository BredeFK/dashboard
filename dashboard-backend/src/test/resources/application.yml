spring:
  application:
    name: dashboard

server:
  address: 0.0.0.0
  port: 0
  ssl:
    key-store: mock
    key-store-password: mock
    key-store-type: mock
    key-alias: mock

app:
  version: 1

cors:
  allowed-origin: mock

location-forecast:
  base-url: mock
  user-agent: mock

nominatim:
  base-url: mock

strava:
  base-url: mock
  club-id: 0
  client-id: mock
  client-secret: mock
  refresh-token: mock

discord:
  base-url: mock
  webhook-url-path: mock

en-tur:
  base-url: mock
  et-client-name: mock
