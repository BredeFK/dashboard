spring:
  application:
    name: dashboard

app:
  version: 1

location-forecast:
  base-url: mock
  user-agent: mock

nominatim:
  base-url: mock

strava:
  base-url: mock
  client-id: mock
  client-secret: mock
  refresh-token: mock

discord:
  base-url: mock
  webhook-url-path: mock